<Page
    x:Class="Drink_Tracker.BillsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Drink_Tracker"
    xmlns:conv="using:Drink_Tracker.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <conv:BoolToVis x:Key="BoolToVis"/>
        <conv:BoolToNonVis x:Key="BoolToNonVis"/>
        <DataTemplate x:Key="BillsItemTemplate">
            <Grid Height="Auto" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Margin="0,0,0,0">
                    <TextBlock Name="ShownName" Text="{Binding Name}" Visibility="{Binding Edited, Converter={StaticResource BoolToVis}}" Style="{StaticResource BaseTextBlockStyle}"/>
                    <TextBox Name="EditName" Text="{Binding Name}" Visibility="{Binding Edited, Converter={StaticResource BoolToNonVis}}"/>
                    <TextBlock Text="{Binding Created}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap"/>
                </StackPanel>
                <AppBarButton Grid.Column="2" Name="EditBillIcon" Visibility="{Binding Edited, Converter={StaticResource BoolToVis}}" Icon="Edit" Click="Edit_Click"/>
                <AppBarButton Grid.Column="2" Name="AcceptEditBillIcon" Visibility="{Binding Edited, Converter={StaticResource BoolToNonVis}}" Icon="Accept" Click="Confirm_Edit_Click"/>
                <AppBarButton Grid.Column="3" Icon="Delete" Click="Delete_Click"/>
            </Grid>
        </DataTemplate>

    </Page.Resources>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Add" Label="New bill" Click="Add_Click" />
            <CommandBar.Content>
                <TextBlock Name="CalculationTitle" Text="Around XX.XX‰. Estimated 0‰ at YY:YY." Margin="12,14"/>
            </CommandBar.Content>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border x:Name="Header" Style="{StaticResource HeaderBackground}" Height="50">
                <TextBlock Name="BillsHeaderTitle" Text="XAccountY" Style="{StaticResource HeaderStyle}"/>
        </Border>
        <ScrollViewer Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <ListView Grid.Row="1" x:Name="BillsList" ItemTemplate="{StaticResource BillsItemTemplate}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="BillsList_ItemClick" ContainerContentChanging="BillsListView_ContainerContentChanging">
                    <!-- ItemsSource="{Binding Source={StaticResource BillsGroupedByAccountsItem}}" -->
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>                
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
