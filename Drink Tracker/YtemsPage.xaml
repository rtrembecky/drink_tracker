<Page
    x:Class="Drink_Tracker.YtemsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Drink_Tracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <CollectionViewSource x:Key="YtemsGroupedByBillsItem" IsSourceGrouped="True" ItemsPath="Ytems" Source="{Binding Accounts, Source={StaticResource AccountsSampleDataSource}}"/>
        <DataTemplate x:Key="YtemsItemTemplate">
            <Grid Height="50" Width="480" Margin="10" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1" Margin="0,0,0,0">
                    <TextBlock Text="{Binding Drink}" Style="{StaticResource BaseTextBlockStyle}"/>
                    <TextBlock Text="{Binding Name}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap"/>
                    <TextBlock Text="{Binding Time_created}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="NoWrap"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="{StaticResource HeaderHeight}"/>
            <RowDefinition Height="{StaticResource SearchBarHeight}"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Border x:Name="Header" Grid.Row="0" Style="{StaticResource HeaderBackground}">
            <TextBlock Text="Ytems obrazovka" Style="{StaticResource HeaderStyle}"/>
        </Border>
        <!-- remove this i guess -->
        <Button x:Name="ButtonAdd" Grid.Row="1" Background="LightGreen" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="ButtonAdd_Click">
            <TextBlock Text="Add" Margin="10"/>
        </Button>
        <!-- up until here -->
        <ListView Grid.Row="2" x:Name="YtemsList" ItemTemplate="{StaticResource YtemsItemTemplate}" ItemsSource="{Binding Source={StaticResource YtemsGroupedByBillsItem}}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="YtemsList_ItemClick" ContainerContentChanging="YtemsListView_ContainerContentChanging">
        </ListView>

        <!-- for future maybe -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="720" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="YtemsList.ItemTemplate" Value="{StaticResource YtemsItemTemplate}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="YtemsList.ItemTemplate" Value="{StaticResource YtemsItemTemplate}"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.Content>
                <TextBlock Text="To pay: XX CZK (Total: YY CZK)" Margin="12,14"/>
            </CommandBar.Content>
            <AppBarButton Icon="Add" Label="New drink" Click="ButtonAdd_Click" />
        </CommandBar>
    </Page.BottomAppBar>
</Page>
