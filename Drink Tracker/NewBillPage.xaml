<Page
    x:Class="Drink_Tracker.NewBillPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Drink_Tracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
            <Border x:Name="Header" Style="{StaticResource HeaderBackground}">
                <TextBlock Text="New bill" Style="{StaticResource HeaderStyle}"/>
            </Border>
            <TextBlock Text="Bill name (e.g. pub):"/>
            <TextBox Name="Bill_name" PlaceholderText="Pivní burza"/>
            <AppBarButton Label="Create" Icon="Accept" Click="Create_click" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Page>
